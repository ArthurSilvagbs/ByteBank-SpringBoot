spring:
  application:
    name: bytebankapi
  datasource:
    url: jdbc:postgresql://localhost:5433/bytebank
    username: postgres
    password: senha123
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update