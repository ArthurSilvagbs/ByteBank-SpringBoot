spring:
  application:
    name: bytebankapi
  datasource:
    url: jdbc:postgresql://localhost:5432/bytebank
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update